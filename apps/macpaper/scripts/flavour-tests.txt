# The suites that read the build's flavour (RELEASES.md, "Pipeline"): the
# official-flavour check runs these and nothing else, the source flavour
# runs everything. MacPaperTests is the app target's suite — licensing
# wiring and enforcement, the keeper while restricted, the updater's wiring,
# the setup guide, preferences. MacPaperCore is compiled the same in every
# flavour. One `swift test --filter` pattern per line.
MacPaperTests
