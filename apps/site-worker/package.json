{
  "name": "@openapps/site-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler d1 migrations apply trial-registry --local && wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler d1 migrations apply trial-registry --remote && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.1.8",
    "@cloudflare/workers-types": "5.20260911.1",
    "typescript": "~6.0.2",
    "vitest": "4.1.11",
    "wrangler": "4.131.1"
  }
}
